#!groovy

pipeline {
  agent any
  stages {
    stage('Docker Compose') {
            steps {
        sh 'cd pipeline-2_files'
        sh ' docker-compose up -f petclinic-docker-compose.yml -d && docker-compose logs -f'
      }
    }
      
  } 
}
