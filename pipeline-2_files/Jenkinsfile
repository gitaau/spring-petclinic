#!groovy

pipeline {
  agent any
  stages {
    stage('Docker Compose') {
            steps {
        sh 'cd pipeline-2_files'
        sh ' docker-compose -f petclinic-docker-compose.yml up -d && docker-compose logs -f'
      }
    }
      
  } 
}
